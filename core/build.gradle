apply plugin: 'java'

sourceCompatibility = targetCompatibility = 1.7

dependencies {
	compile project(':gdx-mod')
	compile 'org.apache.commons:commons-lang3:3.7'
}

jar {
    archivesBaseName = rootProject.name
    classifier = 'core'
}
